Defina o predicado `intersecao/3`, tal que `intersecao(L1, L2, I)`, em que `L1` e `L2` são listas de inteiros, significa que `I` é a lista com os elementos comuns a `L1` e `L2`. Use a negação e o operador de corte sempre que tal melhorar
o seu programa. O seu programa não deve permitir que o pedido de múltiplas respostas origine respostas erradas. Por exemplo:

```
?- intersecao([1,2,3,4,5], [1,3,5], I).
I = [1, 3, 5].
```



O resultado da avaliação deste exercício poderá ser encontrado [aqui](https://gitlab.rnl.tecnico.ulisboa.pt/lp/lp24/feedback/labs/ist1113875/-/tree/master/lab08/negacao_2/README.md), no repositório git de feedback.
