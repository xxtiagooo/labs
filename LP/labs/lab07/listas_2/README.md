Gerando um processo iterativo, implementa o predicado `compMaiorLista/2`,
tal que `compMaiorLista(L, C)`, em que `L` é uma lista de listas, significa que o
maior comprimento das listas de `L` é `C`. Por exemplo:

```
?- compMaiorLista([[1,2,3], [4,3,1,3], []], C).
C=4
```



O resultado da avaliação deste exercício poderá ser encontrado [aqui](https://gitlab.rnl.tecnico.ulisboa.pt/lp/lp24/feedback/labs/ist1113875/-/tree/master/lab07/listas_2/README.md), no repositório git de feedback.
